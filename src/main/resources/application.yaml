spring:
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

  application:
    name: league-api
  datasource:
    url: jdbc:postgresql://localhost:5432/nssl
    username: postgres
    password: 16068676
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  upload:
    dir: uploads